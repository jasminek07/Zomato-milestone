# LLM Provider Configuration
LLM_PROVIDER=groq
LLM_API_KEY=your_api_key_here

# Budget Thresholds for Restaurants (in INR)
BUDGET_LOW_THRESHOLD=500
BUDGET_MEDIUM_THRESHOLD=1500

# Cache Directory (defaults to data/cache inside the workspace)
CACHE_DIR=data/cache

# Candidate shortlist cap for the LLM ranking stage
CANDIDATE_CAP=30
